class Comment{
    constructor(id,user,message,rate,productId)
    {
        this.id = id
        this.user = user
        this.message = message
        this.rate = rate
        this.productId = productId
    }
}

export default Comment